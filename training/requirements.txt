# FlavoraLM training/inference dependencies (CPU-only, local venv only).
# Installed into .flavoralm-venv/ by scripts/setup.mjs — never into global Python.
# Pinned exactly for reproducibility; Python 3.11 only (torch 2.2.x targets numpy 1.x ABI).
torch==2.2.2
# Required by torch (tensor↔numpy bridge) and dataset/eval numeric work.
numpy==1.26.4
